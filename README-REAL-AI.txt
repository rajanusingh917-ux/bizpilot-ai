# BizPilot AI - Real AI upgrade

Upload `api/chat.js` to the repository root inside an `api` folder, and replace the existing `script.js`.

Then add `OPENAI_API_KEY` in Vercel Project Settings → Environment Variables. Never put the API key in GitHub or frontend JavaScript.

After saving the variable, redeploy the project.
